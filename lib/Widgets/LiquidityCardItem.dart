import 'package:flutter/material.dart';

class LiquidityCardItem extends StatelessWidget {
  const LiquidityCardItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListTile();
  }
}
